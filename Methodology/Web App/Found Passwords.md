Hypothetically if you find a password and it contains a year at the end of it but it does not work, you can always try incrementing it

Example
	.db file contains `user:password2021
	web app login fails
	try `user:password2022
	etc...